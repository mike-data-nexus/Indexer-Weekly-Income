{
  "name": "-indexer-index",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ -indexer-index",
    "create-local": "graph create --node http://localhost:8020/ -indexer-index",
    "remove-local": "graph remove --node http://localhost:8020/ -indexer-index",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 -indexer-index",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.35.0",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
